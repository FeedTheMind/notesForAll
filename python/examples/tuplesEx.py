#! python3

t = () # empty tuple
type(t) # <class 'tuple'>

one_elem_tuple = (42, ) # The comma is important; without it, the item is just wrapped in braces

three_elem_tuple = (1, 3, 5)

a, b, c = 1, 2, 3 # Tuple for multi-assignment
